<html>
  <head>
    <title>Bienvenido a Maus Tickets</title>
    <script src="http://ajax.googleapis.com/ajax/libs/angularjs/1.3.10/angular.min.js"></script>
    <script src="js/controller.js"></script>
  </head>
  <body ng-app="flapperNews" ng-controller="MainCtrl" background="img/tickets.jpg">
<div class="panel panel-default">
				<div class="panel-body"><div style="text-align: center">
<form ng-submit="loginForm(user)">
  <input type="text" placeholder="Nombre de Usuario" ng-model="user.user" style="font-size:24px; height:50px"></input>
  <br>
  <input type="password" placeholder="Clave" ng-model="user.pass" style="font-size:24px; height:50px"></input>
  <br>
  <p class="login button"> 
					<input type="submit" value="Login" /> 
				</p>
				<p class="change_link">
					&#191Primera vez en nuestra p&#225gina?
					<a href="register.html" class="to_register">Registrarse</a>
				</p>
</form>
</div>
    <div>
      {{test}}
    </div>
<div ng-repeat="post in posts | orderBy:'-upvotes'">
  <span ng-click="incrementUpvotes(post)">^</span>
  <a ng-show="post.link" href="{{post.link}}">
    {{post.title}}
  </a>
  <span ng-hide="post.link">
    {{post.title}}
  </span>
  - upvotes: {{post.upvotes}}
</div>
</div></div>
  </body>
</html>
